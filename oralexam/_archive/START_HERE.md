# 新窗口从这里开始

> **只读 `LOGIC.md` 一份就够。** 那里是完整的推理链：这套材料为什么长这样、
> 我在路上走错过哪些地方、造一张卡的十步配方、现在到哪了。
>
> `PREMISES.md` 是规则清单（做什么），需要查具体条款时再翻。
> **不要通读这份 START_HERE，也不要回读对话历史**——口令改过很多轮，读全会卡死窗口。

```
cd C:\Users\Admin\Downloads\bio-state-exam\oralexam
claude
```

第一句话贴这个：

> 读 `LOGIC.md`，然后按它 §16 的配方做卡 <编号>。
> 出处层级见 §11，深度基准是 `app/data/01.js`，别读对话历史。

**打开材料**：双击 `app/index.html`。

---

<details>
<summary>以下是 2026-09-11 建立时的原始记录，已被 LOGIC.md 取代，留档备查</summary>


> 建立于 2026-09-11 · oralexam · 只在此目录产出

## 为什么要换窗口

上一个窗口的 cwd 是 `Documents\trae_projects\recombinants_trae_independant`。那棵树的
`CLAUDE.md`（病毒 RBD 重组库）在开窗口时整份进了 context，之后每条消息都会把它连同
全部对话历史重发一次，被内容安全分类器判成 `[bio]`，**在生成中途掐断**。实测：
那棵树 58122 条消息 541 次 `[bio]`；这棵树（`Downloads\bio-state-exam`，顶层没有
`CLAUDE.md`）2886 条消息 0 次。

**把文件写到新目录不解决问题**，污染在窗口的对话历史里，不在硬盘上。只有重开窗口有用。
换模型也没用。

## 开窗口的口令

```
cd C:\Users\Admin\Downloads\bio-state-exam\oralexam
claude
```

第一句话贴这个：

> 读 `README.md`、`ANSWER_SHAPE.md`，然后按 ANSWER_SHAPE 的八格，给
> "Describe the levels of protein structure" 写一张完整样卡给我审。
> 先只写这一张，我审完形状再批量写 32 张。specific 事实从 `book/pages/` 里挖，带页码。

## 已经定下来的

- **只做生化一门**，32 题，题目来自 `../jiri_bank/_qindex.json`（`section == "Biochemistry"`）。
- **唯一事实出处**是 `C:\Users\Admin\Downloads\Biochemie.pdf`（229 页扫描件）。
- **答题形状**见 `ANSWER_SHAPE.md`：主干四格（定义 / 归类 / 机制 / 为什么+停止规则）
  要背出口，储备四格（怎么发现的 / 硬事实 / 陷阱 / 桥）只挡追问。**这份还没被审过。**

## OCR：已经跑完了（2026-09-11）

**229 页全部 OCR 完毕**，`book/pages/p001..p229.txt` 齐了。thin 页只剩 **p001**，是封面，
197 字符，没有内容损失。

p140 曾经是 0 字符——整页被扫成暗底，tesseract 读不出。已经修好并回收（2038 字符），
过程和两个坑记在 `book/BOOK_ERRATA.md` 第 4 条。`tools/ocr_book.py` 现在带一条抢救通道，
以后再遇到暗页会自动处理，记录进 `book/ocr_recovery.json`。

## 已经写完的

`specimen_protein_structure.html` —— 第一张样卡（Describe the levels of protein structure，
题库 #10 / #29 合并），八格全写了，⑥ 三十二条硬事实全部带 p 页码。**等 Ruojin 审形状，
审完才批量写剩下 31 张。**卡片末尾列了要她定的四件事。

写这张卡时挖出一条书本身的错（α-helix 氢键，正文写 n+3、书自己的图画的是 n+4）。
**以后每张卡都要这样核**：关键数字只看 OCR 不算数，回 400 dpi 原图看一眼，
再决定是书印错了还是 OCR 读错了。两种情况处理方式完全不同，都记进 `book/BOOK_ERRATA.md`。

## 2026-09-11 傍晚更新 —— 先读 SHAPE_V2.md

找到了**实测数据**：`recombinants_trae_independant/HANDOFF_stateexam_追问.md`，
2026-09-01 PESB 课程口试的逐层复盘（已过，考官说 terms 要再练）。两条真实追问链，5 层和 6 层，
五个失分点**全部是术语精度，没有一个是"不知道"**——她考前刷完 500 多道题，卡的仍然是这些。

八格不推翻，但要补三件事：兄弟点必须**同一页一口气说完**（拆成翻面卡练的是错的东西）、
每句话必须**自足**（没交代的名词就是你自己给自己开的追问）、
追问链**只在生物学内部往下钻**（咬着上一句的词问，但不许滑进无机化学）。
全部写在 `SHAPE_V2.md`。**PESB 那边的任何内容都不要动**，只把它当考试脉络读。

## 已经定下来的（续）

- **口试用英语**（2026-09-11 定）。逐字稿全英文，捷克术语只作备查，不升主词。
- **事实出处从一层变三层**：捷克书定范围和叫法 → Lehninger 8 补深度和书里缺的章 →
  你自己的实验室管书外那四道题。理由：书**没有氨基酸生物合成这一节**，而且
  Taq/Kapa、fitness landscape、transformation 系列、propeptide 在书里是 0 命中。
- **代谢区（第 5–10 章）要真学，不是过一遍。** ③ 机制那格要写成能教会人的通路走读。

## 还没问 Ruojin 的

1. ~~口试用英语还是捷克语~~ **已定：英语。**
2. 八格会不会太多？现场真正要背的是主干四格，储备格只求眼熟——这个分工她认不认。
3. ⑥ 硬事实挑选标准。**已有部分答案**：这本书正文一个加粗都没有，重点靠节标题、图、
   命名句承载（全书约 500 个锚点，见 SHAPE_V2 §7）。所以改成**命名的反应和酶为主、数字为辅**，
   剩下要她定的只有每张卡收多少条。
4. **书错了的时候听谁的**（样卡里新冒出来的问题）：n+3 那条现在给的是折中说法——先说
   "一整圈之后"，被逼问再给 n+4。要是她知道这位老师就照书讲，就改成书优先。
5. **确切考试日期**（debrief 记的是"约 2026 年 10 月初"，据此排期只剩三周）。
6. **32 题的写作顺序**：按知识空缺排（代谢优先）还是按权重排（w3 优先）。两者不一致。

## 已经挖好的

`book/FOUND_PAGES.md` — 蛋白质结构那题的书页已定位到 p026-p034，
含四级定义、固定每级的力、hydrofobní efekt（= why 链的热力学落点）。
样卡的硬事实直接从那里抄，不用重新找。


</details>
