# CORE HANDOFF — 路由页

Ruojin 定的三条主线。**每条有自己的文件，只读你要做的那一条。**

| 你要做什么 | 打开 | 现状 |
|---|---|---|
| app 功能 | `CORE1_APP.md` | 7 个里做了 **5** 个（2026-08-11 加了 A3 只看必背 filter，**未在浏览器验证**） |
| **Lehninger depth（要写节点就走这条）** | **`CORE2_DEPTH_RUN.md`** ⬅️ **新入口，134 行** | ⚠️ **不要再照 `CORE2_LEHNINGER.md` 的老流程开工**——2026-08-12 实测，depth「一跑就断」的机制是准备阶段先把教材/archive 灌进上下文；新流程用 `prep_depth.py` 把这一步搬进脚本 |
| Lehninger 背景资料（按需 grep，不要整读） | `CORE2_LEHNINGER.md` | **内容 60 / 117 section**（44 深 + 16 浅）；行内引用现在用 `citation_coverage.py` 追踪：**0 个零引用，15 个「薄」**（1-2 条引用撑一整节），见 CORE2 §行内引用 |
| 结构式 | `CORE3_STRUCTURES.md` | ✅ **217 / 217 收官**（2026-08-11；最后 15 个全部从 PubChem 取回，一个都没手画） |

## 进度：先分清「内容」和「层」，这是两个轴

⚠️ **「做了多少」这个问题有两个答案，别混着说。** **内容**＝写了几个新 section 节点；
**层**＝在已有节点上补的东西（`mustKnow`、`trace`、行内引用）。两者进度完全不同。

**内容（新节点）：117 个 section 做了 60 个**，2026-08-09 用两份 tsv 逐个核对过，2026-08-10 又加了 11 个浅节点，2026-08-10 晚开工 depth queue 的光合作用组、第一个是 `L-20-6-1`；2026-08-12 用新的 dossier 流程写了 `L-13-3-1`（§13.3），随后同一天又加了 `L-13-2-1`、`L-13-5-1`、`L-26-1-1`、`L-27-1-1`、`L-7-4-1`、`L-22-1-1`、`L-28-1-1`（7 个，全部 depth queue 出身）。
⚠️ **上面那句话的措辞不要改**——`audit_handoff.py` 用正则 `117 个 section 做了 (\d+) 个`
从这里读实时计数并与数据库比对；改写这一段时它当场报了 `COUNT MISSING`，**说明它在正常工作**，别把它绕过去。

| 来源清单 | 总数 | 已做 | 说明 |
|---|---|---|---|
| `depth_queue.tsv` | 85 | **25** | 原本正好是 rank 第 1–20；2026-08-10 加了 Ruojin 亲自选的**光合作用组** §20.2/§20.3/§20.4/§20.5/§20.6（rank 32/58/54/70/39）——**5 个都是她指定主题，不是按 ratio 取的**。⚠️ §20.5 的 rank 是 70、ratio 只有 1.5，**照 ratio 排根本轮不到它，而它恰恰是唯一有真实考题证据的一组里的一节** |
| `lehninger_only_scope.tsv` | 32 | **27** | = 她圈定的 7 个 + merge 桶 3 个 + 「一笔带过」17 个（ch12 六节 + 2026-08-10 的 11 个）|

**Ruojin 亲自圈定的 13 个已全部处置完毕**：7 个「主要需要的」做完，6 个「merge 进对应 topic」
里 3 个真缺口做完、另 3 个实测确认不必做（§15.1 已覆盖 · §15.3 的「零命中」理由是假的 ·
§19.4 主体已在 `L-23-2-1`）。**所以「她点名要的」现在是零欠账。**

⚠️ **`lehninger_only_scope.tsv` 这条线现在也清空了（2026-08-10）。** 32 个里写了 27 个，
**剩下 5 个全部是实测之后刻意不写的，不是欠账**：§15.1（已覆盖，只有 2 页）· §15.3（「零命中」
理由是假的）· §19.4（主体已在 `L-23-2-1`）· §12.2（Czech `10-18`/`10-19` 已覆盖）·
§12.4（Czech `10-20` 已覆盖）。**不要把 32−27=5 读成待办。**
**唯一还在动的内容线是 depth queue**，做掉光合作用组五节之后还剩 **60 个**。
⚠️ **旧版本这里写「按 ratio ≥1.5 约 38 个」，那是算错的，2026-08-10 实测更正。** CORE2 自己那张
分档表是对的（剩余集合里 **≥3.0 有 13、2.0–3.0 有 25、1.5–2.0 有 12、1.0–1.5 有 10、<1.0 有 5**，
合计 65），**但 13+25+12 = 50，不是 38**。所以 ratio ≥1.5 的池子原本是 **50 个**（做掉光合作用组五节后 45 个）。
**这个数字本身没什么用**——见下面「ratio 不是重要性评分」那一条，**它只是不该是错的**。

**层（在已有 60 个 Lehninger 节点上）：**

| 层 | 进度 | 说明 |
|---|---|---|
| `mustKnow` | **60 / 60** ✅ | 全项目 **270/270**，零缺口（2026-08-12 实测）。**浅节点也有**——她要的就是这个 |
| `points`/`terms`/`quiz`/`oral` | **44 / 44 深度节点** ✅ | ⚠️ 另有 **16 个浅节点有意不带**，不是缺口，见下 |
| **`trace`** | **44 / 44 深度节点** ✅ | 十批收官（2026-08-09→10）；光合作用组五节各自带一张，写的时候就带。全项目 78 张卡 / 77 个节点 |
| 行内引用进检查协议 | ✅ **0 / 60 零引用**（2026-08-12 实测，`citation_coverage.py`） | 全项目 **942 条，0 ELSEWHERE，0 UNCHECKED**（`verify_citations.py`）。**`L-13-3-1` 当天先是零引用落地，同一天后来补上了**（`git log` 里能看到两个提交：先落地、再补 `A p.N`）——这正是 08-12 早些时候写 `L-13-3-1` 那次抓到的教训：**`verify_citations` 全绿只说明「没查出错」，不说明「有内容可查」，一个字段没有引用会被它悄悄跳过而不是报错**。教训没有过期，只是这次的具体节点已经修好。⚠️ **`citation_coverage.py` 是给这个教训配的常设检查**：它把「零引用」（0，当前全项目干净）和「引用薄」（15 个节点只有 1-2 条引用撑一整节的页码范围，不是错误，只是覆盖低）分开报，别再靠 `verify_citations` 一个人判断「够不够」——它从设计上就不回答这个问题。 |
| `explain` / `skipIt` / `bank` | 0 | **有意留空，不要去填**（理由见 CORE2） |

⚠️ **「浅节点」是一种正式的节点类型，不是没写完的节点。** Ruojin 2026-08-10：
「这些都是**浅浅带过 科普性质** must know 有就行」。所以 `brief` 桶的节点**只带
`summary` + `mustKnow`**，**不带** `points`/`terms`/`quiz`/`oral`/`trace`。
`step5_check` 只强制 `id/chapter/section/三个标题/coverage/summary`，**所以这是合法节点**。
**不要「后来把它们补全」**——浅是规格本身。目前 **16 个**：
`L-12-3-1` `L-12-5-1` `L-12-6-1` `L-12-8-1` `L-12-9-1`（2026-08-10 第一批）+
`L-2-1-1` `L-2-3-1` `L-4-5-1` `L-5-2-1` `L-5-3-1` `L-8-4-1` `L-9-3-1` `L-23-4-1` `L-23-5-1`
`L-26-3-1` `L-26-4-1`（2026-08-10 第二批，「一笔带过」收官）。
⚠️ **`L-12-1-1` 是例外**：它写在她说这句话**之前**，所以带全套层，
**那是历史遗留，不是要照抄的模板**。

⚠️ **优先级需要 Ruojin 重新拍板。** 之前的排序是 **CORE2 > CORE3 > CORE1**，理由是 CORE2 里有
她亲自圈定却一个没做的 7 个 section。**那 7 个已于 2026-08-08 全部完成**，所以那条理由用光了。

现在三条线的候选下一步，**成本与收益差别很大**：

- **CORE2**：⚠️ **内容这条轴上，「她点名要的」和「一笔带过」两桶都是零欠账**（2026-08-10
  收官）。**Lehninger-only 32 个里写了 27 个，剩下 5 个全是刻意不写的**，见上面那段。
  **剩下的只有 depth queue 的长尾**（做掉 §20.6 后 64 个，ratio ≥1.5 的 49 个），而 CORE2 自己写着
  **「ratio 不是重要性评分」、这不是待办清单**——**所以这条线做什么必须由 Ruojin 指定主题**。
  ✅ **她已经指定了一次**（2026-08-10，光合作用组 §20.2–20.6，理由见下），**那一组做完之前不需要再问**；
  **做完之后要再问一次**。
  ⚠️ **「层」那条轴是关着的**：`trace` **44/44 深度节点**、`mustKnow` **60/60**、
  `points`/`terms`/`quiz`/`oral` **44/44**、**行内引用 60/60 零缺口**（全项目 **942 条 / 0 ELSEWHERE /
  0 UNCHECKED**）——2026-08-12 实测。
  ⚠️ **这几个分母会随新节点一起长，别把旧快照当现状读**：这一行此前写的是 08-10 的
  `36/36`、`52/52`、`793 条`，那之后光合作用组和 depth queue 又添了 8 个深度节点。
  「关着」指的是**没有欠账**（新节点写的时候就自带这几层），不是指数字不再变。
  **新写的深度节点要自带这几层**（`L-20-6-1` 就是这么写的），**不是事后再补**。
- **CORE3**：✅ **已收官，217 / 217，0 失败**（2026-08-11）。分母定稿于 Ruojin 2026-08-09
  「我看了清单了没关系都做」。全部是从 **PubChem 取回来的**，不是手画的——CID、分子式、
  SMILES、2D 坐标全带出处和日期，每条过五项断言。
  ⚠️ **旧版本这里写「还差 15 个只能手画」，那个判断是错的，2026-08-11 实测推翻。**
  那 15 个查不到，是因为**用了通式名**（`enoyl-CoA`、`phosphatidylinositol`）去查；
  改用**具体化合物名**（`trans-hexadec-2-enoyl-CoA`、具名的 PI 种）之后 **15 个全部查到**。
  **教训与 §20.6 那条 topicKey 的教训是同一条：「查不到」先怀疑自己的查询词，再下结论。**
  ⚠️ **顺带修了一个静默 bug：`app.js` 只渲染五个氨基酸类别的条目，所以 `sugars.js` 那 5 个糖
  在页面上一直是不显示的**（标题写 5/5，底下空的）。检查器只验数据不验渲染，所以从没报过。
- **CORE1**：剩 3 个舒适性功能，不影响考试。A4（笔记+图片）是唯一能损坏已有用户数据的。

⚠️ **`trace`、「一笔带过」、行内引用、光合作用组，四条建议现在全部用完了——CORE2 上没有已知欠账。**
**下一个主题必须由 Ruojin 指定**（见上面「ratio 不是重要性评分」那一条）。
⚠️ **旧版本在这里推荐「CORE3 剩下那 15 个」——那条已于 2026-08-11 做完，CORE3 整条线关闭。**
✅ **真人录音这一桶已于 2026-08-11 收官：59 / 59，0 pending，检查器 0 失败。**
（en.wiktionary 上有英文真人录音的就是这 61 个，其中 `acetone` / `guanine` 在 Commons 上
没有 imageinfo url，取不到，所以上限是 59。）**这一桶不要再看了。**

⚠️ **收官过程推翻了两条写进 handoff 的错误结论，机制值得记住：**
旧版写「换个网络跑一次即可」——**不是网络问题**，是 `upload.wikimedia.org` 的配额 429。
脚本旧版只印 `type(ex).__name__`，屏幕上只有一个没有状态码的 `HTTPError`，**读起来就像网络故障**，
这就是错误结论的来源。**已修：现在印 `HTTP 429, Retry-After 600s`。**
**真实机制比「被限速」更具体**（2026-08-11 实测，一次完整跑里 **25 次 `Retry-After: 1`、5 次
`Retry-After: 600`**）：绝大多数 429 只要等 **1 秒**，但**周期性地**触发一次 **600 秒**重罚，
而**一旦撞上 600 秒，旧的浅重试（3 次、4→8 秒）就再也爬不出来**，那之后每个请求都 429，
整轮剩余全部连锁失败。**这才是「下了 6 个然后全军覆没」的成因，不是网络、也不是单纯的间隔太短。**
所以 `PRON_DELAY=8` 注定无效（8 秒 vs 600 秒），实测立刻重跑拿到 **0** 个。
正确做法是让脚本读 `Retry-After` 并睡够：`PRON_MAX_WAIT=900 PRON_TRIES=6 python tools/fetch_pronunciations.py`
（大部分时间在 sleep，一次就跑完了 47 个）。⚠️ **旧版本这里写过「画了她看不见 = 已完成的工作正在白费」——那是说过头了，
已经改掉。** `biochemie_struct` 是独立的第三个 app，**部署正常、有自己的地址、pro 里也有入口**；
真实情况只是「从根目录那条路（自动跳 basic）走不到它」。**已经画的 25 个没有白费。**

---

## 新 session 的开场指令（复制这段）

```
读 C:\Users\Admin\Downloads\bio-state-exam\CORE_HANDOFF.md，全文读完（只有 8 KB，别跳）。
然后只读你要做的那一条主线对应的那一个 CORE 文件，其余三个不要读。
不要整读 HANDOFF_LEHNINGER.md / HANDOFF.md / LEHNINGER_START.md，只按章节号 grep。
开工前跑 CORE_HANDOFF「每次开工前跑这三条」那套检查；收工前再跑一次 audit_handoff.py。
写完一批立刻 commit + push。被打断就回「继续」，接着做之前先跑一次 parse 检查。

这次做：<在这里填一行>
```

**最后那行填什么**——三条线各自的下一步：

| 想做哪条 | 填这一行 |
|---|---|
| ~~CORE2 的层~~ | ✅ **已于 2026-08-10 全部收官（47/47，698 条引用 0 UNCHECKED）。这一桶不要再看了。** |
| ~~CORE2 内容：一笔带过~~ | ✅ **已于 2026-08-10 收官，17 个全部写完，另 2 个（§12.2/§12.4）实测 Czech 已覆盖故意不写。这一桶不要再看了。** |
| ~~CORE2 内容：光合作用组~~ | ✅ **已于 2026-08-10 收官，§20.2/§20.3/§20.4/§20.5/§20.6 五节全部写完并推送，793 条引用 0 UNCHECKED。这一桶不要再看了。** |
| CORE2 内容：下一个主题（需 Ruojin 拍板） | `depth queue 还剩 60 个，但 CORE2 自己写着「ratio 不是重要性评分、这不是待办清单」，已经有一个 session 因为把 top-20 当队列交出去而让 Ruojin 误以为欠了 65 笔账。所以动手之前先问她要哪一个主题，不要自己按 ratio 排。` |
| ~~CORE3 结构式~~ | ✅ **已于 2026-08-11 收官，217/217，0 失败，0 行 MANUAL。这一桶不要再看了。** |
| CORE1 app | `CORE1 剩 A5（Terms 分页做成抽卡，与 Cards 共享 cardKey）。A4 放最后，它是唯一能损坏已有用户数据的。⚠️ 先在线上站点确认 A3 的 ⭐ 开关显示正常——它没在浏览器里验证过。` |

✅ **上一个 session 停在哪**（2026-08-10，第三次）：**Ruojin 亲自选了 depth queue 里的「光合作用组」
（§20.2–20.6 五节）**，理由是她自己说过的那句话——**「中国是不考光合作用的，但我在捷克参加国考的同学被考了
光合作用」**——这是整个队列里**唯一有真实考题证据**的一组。
✅ **五节全部完成并推送**：`L-20-2-1`（`leh_ch20b.js`）· `L-20-3-1`（`leh_ch20d.js`）·
`L-20-4-1`（`leh_ch20c.js`）· `L-20-5-1`（`leh_ch20e.js`）· `L-20-6-1`（`leh_ch20.js`）。
**这一桶空了，不要再看**——除非 Ruojin 另外指定新主题。
⚠️ **文件名不按小节号排**（20.6 在 `leh_ch20.js`、20.2 在 `leh_ch20b.js`），因为 20.6 先写。
**`index.html` 的五个 `<script>` 标签都加了。**

⚠️ **五节的 topicKey 全部是「并进已有 key」，没有一个新起，而结果分成两种，值得记住：**
四个并进后**产生了新的跨书连接**（`starch-sucrose-biosynthesis` ← §20.6 ·
`photochemical-reaction-centers` ← §20.2 · `co2-assimilation` ← §20.4 ·
`photorespiration-c4-cam` ← §20.5），**跨书连接 21 → 25**；
而 **§20.3 并进 `respiratory-chain`，那个 key 本来就跨书，所以计数一点没涨**——
**这正是 CORE2 早就记过的 `L-19-5-1` 那种形状，不是做错了。**
⚠️ **§20.3 的归属是一个判断，理由写在 `leh_ch20d.js` 的文件头里**：它的主题不是光合作用而是
**ATP synthase 的普适性**，而 ATP synthase 在两本书里都教在 `respiratory-chain`（`6-2-4-5`、`L-19-2-1`），
Lehninger 自己也把机制指回第 19 章的图。**按主题归档，不是按章节归档。**

**下面保留每一节实测出来的写作范围与陷阱，将来若要回头补充照此办理：**
- **§20.2**（A 707–716）：**只写结构与机制**。捷克 `9-9`–`9-13` 已经把命名和拓扑讲厚了（P680/P700/Pheo/
  PQ/PC/Fd/cyt b₆f/Z-schéma 全有）；**零覆盖的是** special pair、exciton transfer、**Mn₄CaO₅ 簇**、
  **Kok 的 S-state 循环**、Tyr_Z、D1 与除草剂结合位点、紫细菌 RC 晶体结构。`9-9` 里 OEC **只有名字没有化学**，那就是入口。
- **§20.4**（A 719–727）：**只写调控**。通路本身捷克 `9-17`–`9-19` 已经很厚（13 步全有、SBPase 也有）；
  **零覆盖的是** rubisco 的**氨甲酰化 + Mg²⁺**、**rubisco activase**、**ferredoxin–thioredoxin 光激活系统**、
  基质 pH/Mg²⁺ 位移、**三碳糖磷酸/Pᵢ 反向转运体**。注意 `thioredoxin` 在捷克层有 14 次命中，**全部在 `4-1-1-2`
  的核糖核苷酸还原酶语境里，与 Calvin 无关**——别被它误导。
- **§20.3**（A 716–719）：**只写旋转催化 + F 型的普适性/演化论证**。捷克 `9-14` 已有 CF₀/CF₁ 与「与线粒体
  ATP synthase 类似」这句类比，`6-2-4-5` 已有结合变构机制；**零覆盖的是** c-ring 化学计量、γ 旋转、
  Boyer/Walker、以及「细菌≈线粒体≈叶绿体」这条演化论证。`chemiosmot` 在整个 ch9 里**一次都没出现**。
- **§20.5**（A 727–732）：**只写 CAM**。光呼吸（`9-20`）和 C-4（`9-21`/`9-22`）都已经很厚，**不要重讲**；
  **CAM 是实测的零覆盖**，而且 `9-22` 把多肉植物**归到了 C-4 名下**，`9-22` 的 quiz 还把「夜间固定 CO₂」
  当成错误选项——**这是一张现成的 trace/conflict 卡**。⚠️ 探针注意：`CAM` 在捷克层 86 次命中**全部是假阳性**
  （cAMP、came、topicKey 里的 `-cam`），`C4` 25 次里几乎全是 C40 类胡萝卜素和 C4 位碳；**用 `C-4` 加人工确认**。
**更早的记录**——**「一笔带过」这一桶已经清空**：
`§2.1 §2.3 §4.5 §5.2 §5.3 §8.4 §9.3 §23.4 §23.5 §26.3 §26.4` **11 个浅节点全部写完并推送**，
分四批提交，每批写完即跑全套检查。新增文件 `leh_ch2b.js` `leh_ch4.js` `leh_ch5b.js`
`leh_ch8c.js` `leh_ch9c.js` `leh_ch23c.js` `leh_ch26b.js`，`index.html` 的 `<script>` 标签都加了。
⚠️ **`§12.2` 和 `§12.4` 在 tsv 里仍标着 `brief` 未做，但那是故意不写的**——实测 Czech
`10-18`/`10-19`/`10-20` 已覆盖，**别再去写它们**。
**同一个 session 接着把最后三个节点的行内引用也补完了**：`L-22-2-1`（14 行）、`L-9-1-1`（14 行）、
`L-9-2-1`（10 行），做法照抄 `L-24-2-1`，全部 OK。**当时是 47/47，698 条引用，0 ELSEWHERE，0 UNCHECKED**。

⚠️ **`L-20-6-1` 这一节实测出两件事，都值得记住：**
1. **`topicKey: starch-sucrose-biosynthesis` 挂在捷克 `6-2-3`（乙醛酸循环）上，看起来像贴错标签，其实不是。**
   **Lehninger 把乙醛酸循环整节放在 §20.6 里面**（A printed pp.735-736，SUMMARY 20.6 也有一条乙醛酸 bullet），
   所以那个 key 一直是对的。并进去之后**跨书连接从 21 涨到 22**（该 key 此前是捷克单书的）。
   **教训：一个「看起来贴错」的 topicKey，先去 A 上查它对应的那一节讲了什么，再下结论。**
2. **tsv 的页码又错了，而且是两端都错**：tsv 写 732-739，实测 **733-740**——printed 732 还属于 §20.5
   （TABLE 20-1 + SUMMARY 20.5），而 **SUMMARY 20.6 从 739 跨到了 740**。这是 tsv 页码出错的第 7 次。
   ⚠️ **本节还有三个 OCR 陷阱**：A 把 α-linked 印成 `a -linked`（这句话根本没法当探针）；
   FIGURE 20-44 的图注印成 `rate-l imiting`，`norm()` 折成 `ratelimiting`，用正常拼写做的探针永远匹配不上；
   **printed 739 的正文与 FIGURE 20-49 的标签逐列交错**，页面上读起来连贯的句子在文字层里并不相邻——
   该页试了五条只成了一条，**那是页面的性质，不是引语的问题**。

⚠️ **这一批实测出来的三个「探针陷阱」，下次写引用前先看一眼**，三个都会让**完全正确的引用**
被报成不可验证，而且报出来的理由会误导人：
1. **引语超过 140 个字符，`QUOTED` 正则直接不匹配**（它只捕 18–140 字符），报的是
   「no searchable phrase」——**看起来像「这个字段里没有引语」，而不是「引语太长」**。
2. **引语里带 `−`（U+2212 减号）**：A 的 OCR 里没有这个字符，整条探针失配。`H+ or OH−`
   这种结尾要切掉。
3. **引语里带连字符复合词**（`three-dimensional`）：A 会把它断行成 `three- dimensional`，
   而 `norm()` 的 `(\w)- (\w)` 规则把它并成 `threedimensional`，于是**再也匹配不上**。
另外：**`coverageNote` 里不要写字面的 `A p.N`**，除非同一个字段里也放了一条已验证的引语——
否则会凭空生成一条没有探针的引用行，直接算 UNCHECKED。写 `A printed p.N` 或 `printed p.N` 即可绕开。

⚠️ **不管填哪一行，都别跳过「动手前先 grep」这一步。** CORE2 里那两张表的覆盖判断
**已经被推翻过五次**，全部是「没查就填」。

⚠️ **「merge 进已有 topic」这一桶已经空了**（§28.3 · §19.5 · §14.5 全做完）。将来如果还有
节点要并进已有 key：`topicKey` **并进、不要新起**，`czTitle` 也**不用** `Žádná —`——照抄
`leh_ch28.js` / `leh_ch19c.js` / `leh_ch14.js`。⚠️ **并进之后跨书连接数「可能涨、也可能不涨」**：
`L-19-5-1` 并进 `respiratory-chain` 时**一点没涨**（那个 key 本来就跨书），而 `L-14-5-1`
并进 `glycolysis` 时**从 19 涨到 20**（那个 key 此前是 Czech 单书的）。**两种都正常。**

---

## 不要读旧的三个 handoff

`HANDOFF_LEHNINGER.md` 302 KB / 4261 行 ≈ 69k tokens，`HANDOFF.md` 68 KB，
**`LEHNINGER_START.md` 8.5 KB**。整读前两个中的任何一个都烧掉一个 session。
**这三个 CORE 文件已经把还有效的结论都抄过来了**，旧文件只在 CORE2 明确指名某个 §
编号时才 grep 那一节。

⚠️ **`LEHNINGER_START.md` 第一行写着「START HERE，读这个然后开工」——那句话现在是过期的。**
它是 CORE 拆分之前的入口，内容比 CORE2 旧。仓库根目录同时躺着五个自称起点的文件
（本文件、LEHNINGER_START、HANDOFF、HANDOFF_LEHNINGER、PAIRING），**只有 `CORE_HANDOFF.md`
是当前入口**。另外 `HANDOFF_LEHNINGER.md` 自己顶上的 banner 写「145 KB ≈ 36k tokens」，
实测是 302 KB——**按本文件的数字，不要按它自己的**。

旧文件为什么不可信：它的 grep 索引漏掉了 §9b，而 §9b 是 Ruojin 对 32 个 Lehninger-only
section 裁决的唯一存放处，`lehninger_only_scope` 这个词在 4261 行里只出现过 1 次。结果是一个
session 得出「Lehninger 没有缺口」并 commit 了（`97a7eb5`），实际上最重要的 7 个一个没做。
**这就是把大文件拆成三个小文件的原因，不要再合回去。**

---

## session 会被打断，这是正常的

打断确实在发生（2026-08-08 写 `L-24-3-1` 时又断了一次）。**处置：回一句「继续」接着做。
每写完一批立刻 commit + push——这是唯一的防线，到目前为止没丢过东西。** 中途被打断后，
先跑一次 parse 检查再往下写，确认文件没停在半句上。

关于原因，要分清两件事，**两条都已于 2026-08-10 用转录记录实测，不要再猜**：

1. **单次输出长度截断——真实存在，但很罕见。** 全部历史里 `stop_reason:"max_tokens"` 只有
   **5 次**。**它不是 loop，是句子写到一半戛然而止**，也是唯一会留下坏 JS 的那种，
   见下面第 4 条规矩。**写长文件时分块 + 断言接缝，就是防它的。**
2. ⚠️ **「打断源是 biosecurity classifier」——这条现在证实了，之前说「无法核实」是错的。**
   **510 条拒绝记录，`apiRefusalCategory` 全部是 `bio`，510/510 无例外**；对照 `refusal`
   出现 **1031** 次、`max_tokens` 只有 5 次，**所以绝大多数中断是拒绝，不是截断**。
   ⚠️ **2026-08-08 判定「查无实据」是因为在 repo 里找**——**数据不在 repo，在
   `~/.claude/projects/*/*.jsonl`**，记录形如
   `{"type":"system","subtype":"model_refusal_no_fallback","apiRefusalCategory":"bio",
   "originalModel":...,"fallbackModel":...}`。**要复查就读那里，不要再翻仓库。**

⚠️ **换模型能让大多数拒绝自愈，这一条推翻了 `LEHNINGER_START.md` 旧版的建议。**
拒绝会被**换到更高一档模型**重试，而数据里 fallback 只有一条路径：**`claude-fable-5 → claude-opus-5`**。
**已经在 Opus 上就没有更高一档可退，所以每次拒绝都是硬停**：
`claude-opus-5` 209 次拒绝挽救 **0** 次（硬中断率 0.75%）· `claude-sonnet-5` 173 次挽救 **0** 次（2.75%）·
`claude-opus-5[1m]` 42 次挽救 **0** 次 · **`claude-fable-5` 86 次挽救 75 次（87%）**。
**用 Fable 5。** 2026-08-10 现场看到：`opus-5[1m]` 在 18:35–19:13 硬停五次，换成 `fable-5` 后
19:29 那次拒绝自动退到 Opus 跑完，完全无感。
⚠️ **两条限制，别过度解读**：这**不**降低拒绝频率，只是让大部分可恢复——Fable 5 仍有 13% 硬停；
而且**它的拒绝「率」在这里测不出来**（assistant 消息不带这个 model tag），**所以只说「可恢复」，
不要说「它拒绝得更少」**。

⚠️ **触发源是题材本身，改措辞没用。** 拒绝在 2026-08-06/07/08 是每天 188/133/128 次，
到 08-09/08-10 掉到 6 次和 14 次——**跟着工作内容走，不跟着措辞走**。**不要再花 session 去
「换个说法绕开它」**（已经烧掉过两个）。

⚠️ **文件太大不是打断的原因。** 四个 CORE 文件加起来 30 KB / 486 行 ≈ 8k tokens，全读一遍
不到一个 session 的 4%；上面那套检查全跑完 15 秒。**这两条都实测过，不要再怀疑它们。**

---

## 每次开工前跑这三条（本 session 实测可用）

```bash
cd C:/Users/Admin/Downloads/bio-state-exam
python -c "import esprima,glob;[esprima.parseScript(open(f,encoding='utf-8').read()) for f in glob.glob('biochemie_pro/data/*.js')];print('parse OK')"
python lehninger_index/scripts/step5_check.py                    # 数据 + schema
python lehninger_index/scripts/verify_citations.py               # 每条 A 页码
python lehninger_index/scripts/audit_handoff.py                  # 这四个 CORE 文件本身
cd biochemie_struct && python tools/check_structures.py && python tools/check_links.py
python tools/check_pronunciations.py            # 真人录音：manifest↔磁盘、署名、只收英文
```

**收工前再跑一次 `audit_handoff.py`。** 2026-08-08 起它会检查这四个 CORE 文件：格式（截断的
段落、不闭合的 `**`、乱序的小节）、**里面的计数是否还等于实时数据**（做了几个 L- 节点、
mustKnow、topicKey），以及 `biochemie_pro/data/` 里每个文件**是否都在 `index.html` 里有
`<script>` 标签**——就是那个「节点在磁盘上存在但 app 里根本不加载、还不报错」的坑，现在它
能抓到了。改完 CORE 文件里的任何数字，跑一次它确认没写歪。

当前基线：`TOTAL: 3`，全部在 `HANDOFF_LEHNINGER.md` 里（一处不闭合的 `**`、一处截断段落、
一处小节乱序），**是旧文件的历史遗留，不是你弄的**。CORE 四个文件和 staleness 都是 clean。

实测（2026-08-10，trace 收官之后）：parse 全部 **40** 个数据文件通过；
`step5_check` exit 0，246 topics；`verify_citations` **648 ok / 0 elsewhere / 0 unchecked**
（第 625 条是 `L-3-3-1` trace 卡里的 `TABLE 3-5, A p.87`——**trace 卡里的页码也会被扫**，
详见 CORE2）；
`check_structures` 25 个结构 0 失败；`check_links` 7 条 0 dead。全套跑完 **15 秒以内**，
**没有任何卡住、超时或 loop 的迹象**。

⚠️ **`step5_check.py` 屏幕上只印一个 `done`。** 真正的结论（`RESULT: all checks pass` 和
每个 app 的节点计数）写在 `lehninger_index/_step5_report.txt` 里——**跑完要 tail 那个文件**，
只看到 `done` 不等于看到了结果。`verify_citations.py` 同理，明细在 `_citation_audit.txt`。

⚠️ **`jsbalance.py` 不在这个仓库里。** 旧 handoff 让你用它，但它在另一个仓库：
`C:\Users\Admin\Documents\trae_projects\recombinants_trae_independant\pesbexplain\tools\jsbalance.py`。
需要时按全路径调用。

---

## 四条硬规矩

1. **有机化学必须落到可观察的后果上。** 原话：我的有机化学几乎说是没有根基…一定不要讲完全
   化学，我听不懂的。不是「4n+2 的离域 π 体系」，而是「环是平的，所以在 280 nm 吸收，所以能
   拿来测蛋白浓度」。
2. **参考数据绝不凭记忆写。** 取来、连出处和日期一起存、用之前先断言。已经抓出过真错误。
3. **不许静默失败。** 每个脚本断言自己的输出，并打印它实际打开了哪个文件。
4. **一批 3 个。** 整章一次写完会撞输出上限，留下半截 JavaScript；这台机器上没有 JS runtime，
   坏掉的表现只是浏览器一片空白。**这条规矩不只适用于「一整章」——单个大节点自己就能撞上限，
   已经发生过两次**：`L-9-2-1` 在 `points` 数组中间断过一次，`L-24-2-1` 在 `oral.model_en`
   里断过一次，而且修复 `L-24-2-1` 时**续写文件本身也连续断了两次**——这不是运气问题，是
   环境对单次输出长度的硬限制，和内容是不是「一整章」无关。**写完任何大文件（或续写任何大段
   文字）后，先跑一次 esprima parse check 或读一遍结尾几行，确认句子完整、括号引号闭合，
   再继续做别的事**——不要假设 Write 工具没报错就等于内容完整。断在哪就从哪接着写，用 Python
   精确拼接（先断言首尾字符匹配再拼）比手动数缩进省事。

## 写入数据文件的方法

用 `parse_nodes` 读节点（**不要 regex 数据文件**），插入脚本**写成文件按路径运行**——这个环境
下 heredoc 会吃掉反斜杠，锚点匹配不上还会报「没找到」明明存在的行。插完断言每个 id 都命中且
没有插两次，再 parse 检查，再 commit。

`parse_nodes` 在 `lehninger_index/scripts/step5_check.py` 里，import 无副作用：

```python
import sys; sys.path.insert(0, r"C:\Users\Admin\Downloads\bio-state-exam\lehninger_index\scripts")
from step5_check import parse_nodes
```

---

## 三个 app 和线上地址

| 目录 | 角色 | storage 前缀 |
|---|---|---|
| `biochemie_basic/` | **冻结**，她实际在看的那个 | `bio.` |
| `biochemie_pro/` | 主力训练器，**240 节点** | `biopro.` |
| `biochemie_struct/` | 结构式 | `biostruct.` |

线上：`https://sumuxie.github.io/bio-state-exam/biochemie_pro/`

⚠️ **站点根目录自动跳转到 `biochemie_basic`**，而 basic 里对 struct 的引用是 **0 次**。从首页
进去没有任何路径能走到结构式——这是「结构式我没看见」的直接原因，详见 CORE3。
