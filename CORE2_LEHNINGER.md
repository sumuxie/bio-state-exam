# CORE 2 — Lehninger 额外内容

**三条主线里最要紧的一条。** 先读 `CORE_HANDOFF.md` 的规矩和打断处置，再看这里。

---

## 全书到底有多少小节

Lehninger 8th ed. 进入 scope 的是 **117 个 numbered section**，拆成两份互不重叠的清单：

| 清单 | 数量 | 含义 | 文件 |
|---|---|---|---|
| depth queue | **85** | Czech 书有对应内容，但 Lehninger 讲得更多 | `lehninger_index/depth_queue.tsv` |
| Lehninger-only | **32** | Czech 书**完全没有**对应内容 | `lehninger_index/lehninger_only_scope.tsv` |

**已写 60 个 L- 节点（44 个深度 + 16 个浅），全部落在上面那 117 个 section 的 scope 之内**（2026-08-09 用两份 tsv
逐个核对过，两份清单零重叠）：**33 个来自 depth queue**（原本正好是 rank 第 1 到第 20；2026-08-10
加了 Ruojin 亲自选的**光合作用组** §20.2/§20.3/§20.4/§20.5/§20.6，rank 分别是 32/58/54/70/39——
**五个都是她指定的主题，不是按 ratio 取的**。⚠️ **注意 §20.5 的 rank 是 70、ratio 只有 1.5，
按 ratio 排根本轮不到它；而它恰恰属于全队列里唯一有真实考题证据的那一组。这是「ratio 不是重要性评分」
最干净的一个实例。**；2026-08-12 又加了 8 个——§13.3 §13.2 §13.5 §26.1 §27.1 §7.4 §22.1 §28.1，
**它们的 rank 恰好是 21–28，所以队列前 28 名至此连续做完**，2026-08-12 实测 rank ≤28 没有一行还空着）；
**27 个来自 Lehninger-only 清单**。

⚠️ **更正一处本 session 自己写错的说法。** 之前这里写「merge 进已有 topic 那 3 个不在两份清单
的任何一份里」——**是错的**。`§28.3`、`§19.5`、`§14.5` **都在 `lehninger_only_scope.tsv` 里**。
「merge 桶」不是第三份清单，**它是 Ruojin 2026-08-06 在这 32 个 Lehninger-only section 内部
做的分类**：她把其中 13 个圈出来，7 个划进「主要需要的」、6 个划进「merge 进对应 topic」。
所以那 10 个 = **她圈定的 7 个 + merge 桶里实测确认的 3 个真缺口**。
⚠️ **Lehninger-only 这条线已于 2026-08-10 清空：32 个里写了 27 个，剩下 5 个全部是实测之后
刻意不写的，不是欠账。** 那 5 个是 §15.1（已覆盖，只有 2 页）· §15.3（「零命中」理由是假的）·
§19.4（主体已在 `L-23-2-1`）· §12.2（Czech `10-18`/`10-19` 已覆盖）· §12.4（Czech `10-20` 已覆盖）。
**不要把 32−27=5 读成待办清单。** 27 个 = 她圈定的 7 个 + merge 桶 3 个 + 「一笔带过」17 个
（ch12 六节 §12.1/12.3/12.5/12.6/12.8/12.9，加 2026-08-10 第二批的
§2.1 §2.3 §4.5 §5.2 §5.3 §8.4 §9.3 §23.4 §23.5 §26.3 §26.4）。

Lehninger-only 已做的 10 个里，「主要需要的」那 7 个是：**`L-9-1-1`（§9.1，克隆/PCR/vector）**、
**`L-9-2-1`（§9.2，CRISPR）**、**`L-22-2-1`（§22.2，氨基酸生物合成）**、
**`L-24-2-1`（§24.2，DNA supercoiling）**、**`L-24-3-1`（§24.3，染色体结构）**、
**`L-25-2-1`（§25.2，DNA repair）**、**`L-25-3-1`（§25.3，DNA recombination）**；
merge 桶的 3 个是 **`L-28-3-1`（§28.3）**、**`L-19-5-1`（§19.5）**、**`L-14-5-1`（§14.5）**。

---

## ✅ 这些已经做过

**20 个 depth 节点，全部带 mustKnow：**
`L-1-3-1` `L-2-2-1` `L-3-3-1` `L-3-4-1` `L-5-1-1` `L-6-4-1` `L-8-2-1` `L-8-3-1` `L-11-2-1`
`L-11-3-1` `L-16-2-1` `L-17-2-1` `L-19-1-1` `L-19-2-1` `L-21-1-1` `L-22-3-1` `L-23-2-1`
`L-23-3-1` `L-25-1-1` `L-26-2-1`

对应 Lehninger §5.1 §8.3 §6.4 §11.3 §21.1 §19.1 §23.2 §16.2 §17.2 §19.2 §11.2 §1.3 §2.2 §3.4
§8.2 §26.2 §22.3 §3.3 §23.3 §25.1。ch3 ch8 ch11 ch19 ch23 各有两个，拆在 `a`/`b` 文件对里。

**其他已完成的层：**

- `mustKnow` **270/270，零缺口**（2026-08-12 实测，`L-28-1-1` 之后）。207 Czech + 60 Lehninger + 3 entity 全有。
- 3 张 entity 卡：`E-tryptophan` `E-histidine` `E-cysteine`。下一张的测量结论是 `heme`。
- `topicKey` **78** 个，**31 个跨书连接**（实测，2026-08-12，`L-28-1-1` 之后；历史见下）。
  ⚠️ **第 26–31 个连接，2026-08-12 这一批 8 个节点里有 6 个产生新连接**（实测，不是按增量推算）：
  `L-13-3-1`（§13.3）并进本来单书的 `atp-and-phosphoryl-transfer`（Czech `6-1-2-1`/`6-1-2-2`）·
  `L-13-2-1`（§13.2）并进 `biochemical-reaction-logic`（Czech `10-1..10-5`、`6-1-3`）·
  `L-13-5-1`（§13.5）并进 `metabolic-regulation`（Czech `10-12..10-16`）·
  `L-27-1-1`（§27.1）并进 `genetic-code`（Czech `4-2-1`）·
  `L-7-4-1`（§7.4）并进 `glycoconjugates`（Czech `7-5`）·
  `L-22-1-1`（§22.1）并进 `nitrogen-metabolism`（Czech `9-23..9-26`）。
  同批另外两个**没有**产生新连接：`L-28-1-1`（§28.1）和 `L-26-1-1`（§26.1）都并进了**本来就跨书**的
  key（`gene-regulation-proteins` 已有 `L-28-3-1`；`transcription` 已有 `L-26-2-1`/`L-26-4-1`）——
  与上面 `L-19-5-1` 的形状相同，是「加深」不是「新连」。
  之前的 **25 个**（2026-08-10 光合作用组之后）：
  ⚠️ **第 22–25 个来自光合作用组，四个都是「并进已有 Czech 单书 key」**：
  `starch-sucrose-biosynthesis`（§20.6）· `photochemical-reaction-centers`（§20.2）·
  `co2-assimilation`（§20.4）· `photorespiration-c4-cam`（§20.5）。
  **而同组的 §20.3 并进 `respiratory-chain` 时计数一点没涨**——那个 key 本来就跨书，
  **是 `L-19-5-1` 的形状，不是做错了**。⚠️ **`starch-sucrose-biosynthesis` 这一条另有一个教训**：
  它挂在 Czech `6-2-3`（乙醛酸循环）上，**看起来像贴错标签，其实不是**——
  **Lehninger 把乙醛酸循环整节放在 §20.6 里面**（A printed pp.735-736，SUMMARY 20.6 也有一条乙醛酸 bullet）。
  **一个「看起来贴错」的 topicKey，先去 A 上查它对应的那一节讲了什么，再下结论。**
  ⚠️ **第 21 个跨书连接来自 `L-5-2-1`（§5.2 免疫球蛋白）并进 `protein-tertiary-quaternary`**：
  那个 key 此前**只有 Czech 节点**（`2-2-3`、`2-2-4`），所以这一并**产生了新连接**——
  是 `L-14-5-1` 并进 `glycolysis` 的形状，不是 `L-19-5-1` 并进 `respiratory-chain` 的形状。
  合并的理由是实测的：整个捷克层关于抗体只有 `2-2-3` 的**一句话**（它自己的 `cz` 字段只写
  「kontakt podjednotek」），而那句话讲的正是「抗原结合位点由 H 链和 L 链共同构成」——
  **§5.2 就是那句话的完整版**。
  第二批新起的 6 个**单书** key：`water-and-weak-interactions`（§2.1）、`motor-proteins`（§5.3）、
  `genomics`（§9.3）、`obesity-and-body-mass`（§23.4）、`diabetes-mellitus`（§23.5）、
  `rna-dependent-synthesis`（§26.3）；另外 4 个并进**本来就跨书**的 key（§2.3→`amino-acids`、
  §4.5→`working-with-proteins`、§8.4→`nucleotides`、§26.4→`transcription`），所以那 4 个不动计数。
  ⚠️ **`diabetes-mellitus` 故意没有并进 `fatty-acid-oxidation`**，尽管那个 key 里的 Czech
  `8-4-4-3`/`10-8` 正是全捷克层唯一提到 diabetes 的地方（13 次，全部是「它导致酮体过量」）：
  那个 key 的主题是 β-氧化和酮体，**为了凑一条跨书连接而把一个疾病归档到那里，是归错档**。
  联系改成在正文里点名那两个节点。**「能连」和「该并」是两件事，这一条以后照办。**
  ⚠️ **同样是「并进已有 key」，对计数的影响可以完全不同，这一点值得记住。**
  `L-14-5-1`（§14.5）并进 `glycolysis`，而那个 key 此前**只有 Czech 节点**（`7-8-1/2/3` 加
  `10-6`/`10-7`），所以它**产生了第 20 个跨书连接**。而 `L-19-5-1`（§19.5）并进
  `respiratory-chain` 时**两个数字一个都没变**：该 key
  本来就已经是那 19 个之一（Czech `6-2-4-1..5` + `L-19-1-1` + `L-19-2-1`），现在解析到 **8**
  个节点。**「merge 进已有 topic」不一定产生新的跨书连接——它也可以只是加深已有的那一条**，
  而这同样是这个桶要的效果。写这一桶剩下的 §14.5 时不要因为「计数没涨」就以为做错了。
  ⚠️ **第 19 个是新类型**：`L-28-3-1`（§28.3）**没有新起 key，而是并进了 Czech 已有的
  `gene-regulation-proteins`**，于是该 key 现在解析到 `4-2-4-1` + `4-2-4-2` + `L-28-3-1`。
  这正是「merge 进已有 topic」桶该有的做法，与前七个刻意单书的 Lehninger-only 节点相反。
  **写这一桶剩下的节点时照此办理，不要新起 key。**
  第 70 个是 `dna-recombination`（`L-25-3-1`），单书 key，故意不并进 `dna-repair`。
  这是 pro 存在的意义。第 69 个是 `dna-repair`（`L-25-2-1`），第 68 个是
  `chromosome-structure`（`L-24-3-1`），都是单书 key。`chromosome-structure` 故意**不**并进
  `dna-supercoiling`：24.2 是 DNA 拓扑，24.3 是包装它的蛋白构架，两者的联系写在 `L-24-3-1`
  的 points 4 和 12 里，而不是靠合并 key 来表达。`dna-repair` 故意**不**并进 `dna-replication`：
  合成过程中的校对和事后对损伤的修复是**两件事、两套酶**，从 `4-1-3-1` 点「同主题」的读者
  想看的是复制叉，不是糖基化酶；真正重叠的那一处（聚合酶 I 在切除后填补缺口）写在
  `L-25-2-1` 的 points 里。第 64–67 个是
  `recombinant-dna-technology`（`L-9-1-1`）、`exploring-protein-function`（`L-9-2-1`）、
  `amino-acid-biosynthesis`（`L-22-2-1`）、`dna-supercoiling`（`L-24-2-1`），**都故意是单书
  key**：Lehninger-only 的节点没有 Czech 节点可连，这是预期行为不是缺陷，后续每个
  Lehninger-only 节点都会这样。注意 `amino-acid-biosynthesis` 和已有的 `amino-acids`
  （结构/性质）、`amino-acid-degradation`（降解，ch5）是三个不同的 key；`dna-supercoiling`
  同理没有并进已有的 `dna-replication`（那个 key 是复制叉机制——聚合酶/解旋酶/连接酶，
  超螺旋是拓扑与压缩，虽然拓扑异构酶两边都要用到，但主题不同，故意分开。
- UI 全部完成：两本书都能渲染、侧栏「分书 / 按主题」切换、「同主题」条、`lehNotes` 块。
  **不需要再建任何东西。**
- 引用 **942 条全 OK，0 ELSEWHERE，0 UNCHECKED**（2026-08-12 实测；636 → 648 → 660 → 698 → 726 → 751
  → 766 → 781 → 793 → 942，最后一跳是 08-12 这批 8 个节点，含 `L-13-3-1` 补齐）。
  ⚠️ **`L-13-3-1` 曾经一条行内引用都没有——2026-08-12 当天先落地、后补上**：落地时
  `leh_ch13.js` 里 `A p.N` 命中 0，`verify_citations` 却照样报全绿，因为**没有引用可查，
  不是引用都对**；同一天用 `L-24-2-1` 的写法补了 41 处引用才算数。⚠️ **这是本项目第二次踩
  「检查器沉默等于通过」，教训不会因为这个具体节点修好就过期**：`verify_citations` 从设计上
  就不检查「有没有引用」，只检查「已有的引用对不对」。为了不必每次都靠人肉发现零引用节点，
  08-12 补了 `citation_coverage.py` 专门测这件事——现在测出**全项目 60 个 lehninger 节点，
  0 个零引用**（干净），但 **15 个「薄」**（1-2 条引用撑一整节声称的页码范围，不算错，只是覆盖低，
  见 `_citation_coverage.txt` 的名单）。**「薄」不是要立刻去补的缺口，是留给以后想加深某节时的线索。**
  以下是重新打开之前的记录：行内引用那一轮补写于 2026-08-10 收官
  （最后三个是 `L-22-2-1`、`L-9-1-1`、`L-9-2-1`）；此后新写的光合作用组五节**写的时候就在协议内**。
  **这一层不用再看了。**
  ⚠️ **光合作用组实测出一条新的操作教训**：`L-20-2-1` 第一次跑 `verify_citations` 报了 **1 UNCHECKED**——
  `points[9]` 引了 `A p.713` 却**没有在那个字段里放引语**。**修法是往该字段里补三条验过的引语，不是把引用删掉。**
  **写完先跑一次检查器再 commit，这一条就永远只花一分钟。**
  做法（将来加新节点照抄）：在选中的 `points[].en` **末尾追加**一段
  `IN THE BOOK'S OWN WORDS: “…” (A p.N); “…” (A p.M).`，**只动 `en`、不动 `cn`**，
  **纯追加、不改原文**；每一条引语都先在 A 上搜过、页码取搜索返回值。
  ⚠️ **同一个字段里的每一个页码都必须有一条落在该页上的引语**——检查器按 (页, 字段) 建行，
  借用邻近字段的引语会被降级。插入用带断言的脚本：锚在该 point `en` 的**末尾 70 字符**上、
  断言全文只命中一次、断言没有重复追加、**写盘前先 esprima parse**。
  三个节点的页码范围实测全部落在各自已声明的 `pages` 之内（805-816 / 301-316 / 317-326），
  **没有出现 `L-24-2-1` 那种范围写错的情况**。
  **保持在零。** 这套检查只扫**行内引用**——`points` 每条正文里嵌一个 `(A p.N)`，靠紧挨着它
  的**图表标号**或同一字段里的**逐字引语**当探针。
  - ⚠️ **`trace` 卡里的引用同样会被检查器扫到，这一点第三批才发现。** 写 `L-3-3-1` 时
    `numbers` 里放了一句「表 3-5 …（A p.87）」，当场变成 **1 UNCHECKED**：`FIGREF` 只认
    拉丁文的 `FIGURE / Fig. / TABLE / Tab. / Box` + 编号，**中文的「表 3-5」它读不到**。
    改写成 `TABLE 3-5` 之后即 `label found on [87]`，总数从 624 涨到 625。
    **结论：trace 卡里要么不写页码，要么就按下面那四条规矩写**（探针必须在它自己那个字段里）。
    第四批起的二十一张卡因此都没有写页码。
  - ✅ **`L-24-3-1`（27 条）、`L-25-2-1`（27 条）、`L-25-3-1`（28 条）、`L-28-3-1`（31 条）、
    `L-19-5-1`（27 条）、`L-14-5-1`（26 条）完整参与这套协议**，每一条引用都被验证过。
  - ✅ **`L-24-2-1` 已于 2026-08-10 补完行内引用并进入协议**：7 个 points 共嵌入 **20 条逐字
    引语**，检查器按 (字段, 页) 去重后是 **11 行，全部 OK**。总数 625 → **636**。
    ⚠️ **补的过程中查出一个真错误，这正是「不在协议里」会掩盖的那类**：该节点原本声明
    `pages: [890..898]`、`coverageNote` 也写 pp.890-898，**但 A p.890 是 24.1 的尾巴**——
    §24.2 开在 **p.891**（「24.2 DNA Supercoiling / How is cellular or viral DNA compacted…」），
    SUMMARY 24.2 在 **p.898**。已改成 **891-898**。**页码没被机器验过，就会这样错着躺很久。**
  - ✅ **`L-9-1-1`（14 行）、`L-9-2-1`（10 行）、`L-22-2-1`（14 行）已于 2026-08-10 补完并进入协议，
    至此 47/47。** 在此之前它们只在 `coverageNote` 给了整段页码范围——那不是错误，但**当时的
    636/648/660 这些数字并不覆盖那三个节点**，所以那时不能说「全项目都核对过」。**现在可以了。**
    做法（照抄 `L-24-2-1`，将来加新节点沿用）：先用检查器自己的页索引 `doc[printed + 36 - 1]`
    把每句话在 A 上搜出来、拿它返回的页码，**绝不要从 A↔B 映射推**；引语用**弯引号** `“…”`
    （字段是双引号 JS 字符串，检查器的 `QUOTED` 认弯引号，不用转义）。
  - **写新节点时怎么让引用能被验证**（`L-24-3-1` 实测出来的四条）：① 引语至少 **4 个词**，
    3 个词会被判 UNCHECKED；② 每条 `A p.N` 在**它自己所在的字段**里要有自己的探针，
    借用同字段另一条引用的引语会被降级；③ 图表标号必须是紧挨在引用**之前**的那一个；
    ④ **A 是扫描 OCR，图形里的文字读不到**——`FIGURE 24-30`、`FIGURE 24-31`、`BOX 24-2`
    的标号在 A 的文字层里根本不存在，这类只能靠正文引语来验。
  - **2026-08-10 第二批浅节点又实测出三条，全部会让「完全正确的引用」被报成不可验证，
    而且报出来的理由是误导性的**：
    ⑤ **引语必须在 18–140 个字符之间**。`QUOTED` 正则写死了 `{18,140}`，**超长的引语
    根本不被捕获**，报出来是「no searchable phrase」——**读起来像「这个字段里没有引语」，
    而不是「引语太长」**。`L-26-3-1` 第一版用了 161 字符的引语，就是这样失败的。
    ⑥ **引语里不能带 `−`（U+2212 减号）**——A 的 OCR 里没有这个字符。`H+ or OH−` 这种
    结尾必须切在它前面。`L-2-3-1` 第一版栽在这里。
    ⑦ **引语里不要带连字符复合词**。A 会把 `three-dimensional` 断行成 `three- dimensional`，
    而 `norm()` 的 `(\w)- (\w)` 规则把它并成 `threedimensional`，于是探针**再也匹配不上**。
    §4.5 的开头句就是这样测不到的（那句话在 p.136 上一字不差）。
  - ⚠️ **`coverageNote` 里不要写字面的 `A p.N`**，除非同一个字段里也放了一条已验证的引语。
    否则会生成一条**没有探针的引用行**，直接算 UNCHECKED。写 `A printed pp.X-Y` 或
    `printed p.N` 就绕开了——`CITE` 正则要求 `A` 后面紧跟空白再跟 `pp?.`，中间插一个词就不匹配。
    `leh_ch12b.js` 一直是这么写的，第二批第一次没照做，当场多出 3 条 UNCHECKED。
- `trace` **78 张卡（77 个节点）**：Czech 层 34 张 / 33 个节点，**Lehninger 层 44 张 / 44 个节点**
  ——即**每个深度节点各一张**（44 个深度节点，44 张卡），浅节点不带。
  （⚠️ **这一行落后过两次，两次同一个原因**：trace 十批「收官」之后写的深度节点**写的时候就自带
  trace**，没人回来改这个总数，于是「收官」这个词让人以为数字也冻结了。第一次是 2026-08-10：
  原写「64 张 / 63 个节点，Lehninger 30 / 30」，少算了 `L-12-1-1`，改成 65 / 64、31 / 31。
  第二次是 2026-08-12：光合作用组和 depth queue 又添了 13 个深度节点，65 → 78 张、64 → 77 个节点，
  Lehninger 31 → 44。实测口径：`sum(len(n['trace']))` = 78 张，带 trace 的节点 = 77 个，
  2026-08-12 实测。**CORE_HANDOFF 那边的 78 张 / 77 个节点是对的，是本文件又落后了一格——
  「收官」指的是那十批补完了，不是指这个数不再增长。**）
  （第一批 `L-14-5-1` 果糖-2,6-二磷酸 · `L-19-5-1` heteroplasmy · `L-28-3-1` 转录基态；
  第二批 `L-5-1-1` 血红素 · `L-19-1-1` 呼吸链抑制剂 · `L-16-2-1` 草酰乙酸的催化量；
  第三批 `L-24-2-1` linking number · `L-3-3-1` specific activity · `L-1-3-1` 质量作用比 Q；
  第四批 `L-6-4-1` 过渡态 vs 中间物 · `L-25-1-1` processivity · `L-17-2-1` 肉碱穿梭；
  第五批 `L-2-2-1` pKa · `L-11-3-1` 电化学梯度 · `L-9-2-1` CRISPR 是一份档案；
  第六批 `L-24-3-1` 核小体 · `L-25-2-1` 链识别 · `L-9-1-1` 限制性内切酶；
  第七批 `L-19-2-1` 能量花在放开 ATP 上 · `L-25-3-1` RecA 是一条丝 · `L-8-3-1` 变性与 Tm；
  第八批 `L-21-1-1` 柠檬酸穿梭 · `L-23-2-1` 葡萄糖激酶的 Km · `L-26-2-1` 核酶；
  第九批 `L-8-2-1` 堆积而非氢键 · `L-11-2-1` flip-flop · `L-23-3-1` β 细胞用糖酵解当传感器；
  第十批 `L-3-4-1` Leu/Ile 分不开 · `L-22-2-1` 序贯反馈抑制 · `L-22-3-1` 降解产物不是废物，
  2026-08-09 开工、**2026-08-10 收官**）。
  ✅ **44 个深度节点全部带 trace，这一层不用再看了**（2026-08-12 实测；这一行就是上面那条
  「落后过两次」的第三个实例——它还停在 30，而十批之后又添的深度节点写的时候就自带 trace）。
  Czech 层分布如下。分布：ch1 6→2 · ch2 10→7 · ch3 9→5 · ch4 18→4 ·
  **ch5 10→0** · ch6 19→6 · ch7 42→7 · **ch8 40→0** · **ch9 27→0** · ch10 26→3。
  ch5/ch8/ch9 为零是 Ruojin 的决定（densify 她正在读的，不要摊薄），**不是要填的洞**。

**有意留空、不要去填的：** `explain` 0（被 `trace` 取代，2026-08-08 她选了 trace 不是
takeaway）· `skipIt` 0（在找到替代 RECAP 的证据来源之前禁止写）· `bank` 0（题源循环控件会
明说自己是空的，不是静默失效）。

---

## ✅ Ruojin 亲自圈定的 13 个：「主要需要的」7 个已全部做完

32 个 Lehninger-only section 里，**她点名「主要需要的」那 7 个（§9.1、§9.2、§22.2、§24.2、§24.3、
§25.2、§25.3）于 2026-08-08 全部做完**。⚠️ **这一行原来结尾写「还剩 25 个」，那是 08-08 当天的
状态，2026-08-12 已过期**：整条 Lehninger-only 线在 2026-08-10 就清空了，**实测 32 个里写了 27 个，
剩下 5 个是刻意不写的、不是欠账**（§15.1 §15.3 §19.4 §12.2 §12.4，理由见本文件开头）。
她在 2026-08-06 对这 32 个逐一裁决过，裁决记在旧 archive 的 §9b。其中 13 个是明确要做的：

### 「主要需要的」7 个（88 印刷页）

| § | A 页码 | 内容 | Czech 层是否已有 |
|---|---|---|---|
| ~~9.1~~ | 301–316 | 克隆、restriction enzyme、vector、**PCR**、library | ✅ **已做 `L-9-1-1`** |
| ~~9.2~~ | 317–326 | 蛋白功能研究，**CRISPR 在这里** | ✅ **已做 `L-9-2-1`** |
| ~~22.2~~ | 805–816 | 氨基酸的生物合成 | ✅ **已做 `L-22-2-1`** |
| ~~24.2~~ | 890–898 | DNA supercoiling | ✅ **已做 `L-24-2-1`** |
| ~~24.3~~ | 898–910 | 染色质与染色体结构 | ✅ **已做 `L-24-3-1`** |
| ~~25.2~~ | 930–940 | DNA repair | ✅ **已做 `L-25-2-1`** |
| ~~25.3~~ | 940–956 | DNA recombination | ✅ **已做 `L-25-3-1`** |

**这七个已于 2026-08-08 全部完成。** 下一步的优先级需要 Ruojin 重新拍板，见 `CORE_HANDOFF.md`。

### ⚠️ 这一列曾经连错三次，全部在 2026-08-08 一天内抓到

**它原来写的东西不可信，已经逐格重测过。** 三处错误都出自同一次未经验证的填写：

| § | 原来写的 | 实测 |
|---|---|---|
| 24.3 | `4-1-4-1` 有涉及 | ❌ `4-1-4-1` 讲的是**转录和 RNA 种类**（`topicKey: transcription`）。`nukleozom`/`nucleosom`/`centromer`/`heterochromatin`/`euchromatin`/`chromozom` **全部 0 命中**。覆盖为**零** |
| 25.2 | `4-1-3-1` 有涉及 | ❌ `oprav` 0、`reparac` 0、`fotolyas` 0、`glykosylas` 0、`AP-endonukleas` 0、`xeroderma` 0。17 处 `excis` 里**16 处**是 RNA 引物切除／内含子剪接／胰岛素 C 肽切除。唯一的 `SOS` 命中是 **"LYSO`SOS`MES"** 里的子串 |
| 25.3 | `4-1-2` `4-1-3-1/2` 有涉及 | ❌ `rekombinac` 0、`Holliday` 0、`RecA` 0、`RecBCD` 0、`transpozon`/`transposon` 0。Czech 层**只有一句**：`4-1-3-2` 的连接酶条目说它「在真核生物遗传重组中拼接 DNA 链」 |

**教训不是「这张表有三个笔误」，而是「整列都是没查就填的，所以整列都不能信」。** 25.3 这一
格现在写的是实测结果；如果将来有人给这张表加行，**加的时候就 grep，不要留给下一个 session**。

**§24.2 也核实过**：Czech 节点 `4-1-3-1` 只有一条术语级别的定义——「环状DNA进一步自身扭曲的
形式」一句话——没有 linking number、没有拓扑异构酶机制、没有超螺旋密度公式，`L-24-2-1` 写
之前已确认这就是 Czech 层的全部内容。`topicKey` 故意新起 `dna-supercoiling`，没有并进
`4-1-3-1` 自己的 `dna-replication` key，因为超螺旋/拓扑和复制叉机制是两个不同的主题
（详见上面「已经做过」那节的说明）。

**§9.1 和 §9.2 曾是彻底的空白，本 session 用原始文件 grep 验证过**：Czech 的 10 个数据文件里
`CRISPR` `Cas9` `PCR` `klonov` `restrikč` `plasmid` `plazmid` `knihovn` `sekvenov` **全部 0 次
命中**。（ch10 那 10 处 `Sanger` 是胰岛素测序史 Sanger 1953，与分子生物学方法无关。）建
`L-9-1-1`/`L-9-2-1` 之前，整个项目里 CRISPR 只出现 1 次、PCR 24 次，全在 `leh_ch8.js` 一个文件
里，是 L-8-3-1「核酸化学」顺带提到的，不构成 §9.1/§9.2 的覆盖。**§22.2 也核实过**：Czech
节点 `10-2` 只讲分解代谢/合成代谢的通用三阶段框架（p.218 一页），用氨基酸举了一句例子，没有
任何具体路径，`L-22-2-1` 写之前已确认这就是 Czech 层在这个主题上的全部内容。

**§24.3 也核实过**，而且核实的结果推翻了这张表原来写的东西——见上面那段 ⚠️。Czech 层对
染色体结构的覆盖是**零**：核小体、着丝粒、异染色质、常染色质、染色体这些词一个都没有。
`L-24-3-1` 因此是从零开始写的，13 points / 16 terms / 4 quiz / 1 oral，A pp.898–910。

**§25.2 也核实过**，结果同样推翻了表里原来写的（见上表）。`L-25-2-1` 从零写起，
15 points / 15 terms / 4 quiz / 1 oral，A pp.930–940，**27 条引用全部过检**。

**§25.3 也核实过**，同样从零写起。`L-25-3-1`（`leh_ch25c.js`）14 points / 16 terms / 4 quiz /
1 oral，A pp.940–955，**28 条引用全部过检**。它接上了 `L-25-2-1` 留下的三条线头（重组修复是
双链断裂的准确出路、RecA 是 pol V 的组分、BRCA2 装载 Rad51），没有重讲。

**至此，Ruojin 圈定的「主要需要的」7 个全部完成。**

### 「merge 进对应 topic 正常展开」6 个（43 页）

不是新主题，是 Czech 书已教路径的调控层，所以「一笔带过」是错的桶。

⚠️ **这张表 2026-08-08 逐格重测过，6 行里 4 行有问题**——两行的 Czech 覆盖判断是错的，
另外两行**连 Lehninger 的小节标题都写错了**（19.4/19.5 都被写成「氧化磷酸化 topic」，
它们不是）。下面是**实测**版本，页码经核对无误：

| § | Lehninger 真实标题 | A 页码 | 实测状态 |
|---|---|---|---|
| ~~14.5~~ | Coordinated Regulation of Glycolysis and Gluconeogenesis | 539–546 | ✅ **已做 `L-14-5-1`**。零命中 2026-08-09 复核成立（`fruktosa-2,6`/`PFK-2`/`果糖-2,6`/`bifunkč` 全 0；Czech 有 `fosfofruktokinas` 4 次即 PFK-1 本身，`xylulosa-5-fosf` 6 次但只作为 PPP 中间物、从不作为调节物） |
| 15.1 | The Structure and Function of Glycogen | 557–558 | ✅ **已覆盖，且只有 2 页**。Czech `7-4-1-1`（储存多糖）+ `7-11-2-1/2`（糖原磷酸解）。**价值最低** |
| 15.3 | Coordinated Regulation of Glycogen Breakdown and Synthesis | 565–571 | ❌ 原写「全项目零命中」是**错的**。Czech 节点 `7-12-2` 标题就是「糖原的合成与分解——**激素级联调控**」；`糖原合酶` 28、`磷酸化酶激酶` 22、`胰高血糖素` 18 |
| 19.4 | **Mitochondria in Thermogenesis, Steroid Synthesis, and Apoptosis** | 689–691 | ❌ 原写「氧化磷酸化 topic」是**标签错**。产热部分**已被 `L-23-2-1` 覆盖**（UCP ×18、棕色脂肪 ×15、解偶联蛋白 ×3），`L-19-1-1` 也有。剩类固醇合成与凋亡 |
| ~~19.5~~ | **Mitochondrial Genes: Their Origin and the Effects of Mutations** | 692–696 | ✅ **已做 `L-19-5-1`**。零命中的判断 2026-08-09 又复核了一次并成立（`mtDNA`/`mateřsk`/`heteroplaz`/`homoplaz`/`endosymbi`/`Leber`/`MERRF` 全 0；3 处 `mitochondriáln` 是基质、脂肪酸延长、内膜类比） |
| ~~28.3~~ | Regulation of Gene Expression in Eukaryotes | 1075–**1093** | ✅ **已做 `L-28-3-1`**。判断准确（六行里唯一准确的一行），且是最大的真缺口（17 页）|

**结论：这 6 个里真正该做的是 3 个——§28.3（17 页）、§19.5（5 页）、§14.5（8 页），✅ 现在全部做完。这一桶空了。**
§15.1 已覆盖且只有 2 页；§15.3 的「零命中」理由是假的；§19.4 的主体内容已在 `L-23-2-1` 里。

⚠️ **写这一桶的节点，`topicKey` 要并进 Czech 已有的 key，不要新起。** `L-28-3-1` 用的是
`gene-regulation-proteins`（Czech `4-2-4-1`/`4-2-4-2` 的 key），于是产生了**第 19 个跨书
连接**——这正是「merge 进对应 topic」这个桶的**全部意义**，与前七个刻意单书的 Lehninger-only
节点**做法相反**。`czTitle` 也随之**不用** `Žádná —`，改走 depth 节点惯例（照抄 `L-25-1-1`）。

⚠️ **§28.3 的页码要注意**：A↔B 映射给的是 1075–1092，但 **SUMMARY 28.3 实测在 A p.1093**，
所以节点覆盖的是 **1075–1093**。这是「页码必须在 A 自己的 OCR 里测，不能信映射」的又一个例子。

### ✅ 「一笔带过」19 个：已于 2026-08-10 收官（写了 17 个，2 个刻意不写）

原话「其他的最简单一笔带过就行」。19 个是：整个 ch12（§12.1/12.2/12.3/12.4/12.5/12.6/12.8/12.9）、
§5.2–5.3、§23.4–23.5、§26.3–26.4、§2.1、§2.3、§4.5、§8.4，以及 §9.3 基因组学
（她另外说过「基因组学可以简单讲」）。

| 批次 | 节点 | 文件 |
|---|---|---|
| 2026-08-10 第一批 | `L-12-1-1`(深) `L-12-3-1` `L-12-5-1` `L-12-6-1` `L-12-8-1` `L-12-9-1` | `leh_ch12.js` / `leh_ch12b.js` |
| 2026-08-10 第二批 | `L-2-1-1` `L-2-3-1` `L-4-5-1` `L-5-2-1` `L-5-3-1` `L-8-4-1` `L-9-3-1` `L-23-4-1` `L-23-5-1` `L-26-3-1` `L-26-4-1` | `leh_ch2b.js` `leh_ch4.js` `leh_ch5b.js` `leh_ch8c.js` `leh_ch9c.js` `leh_ch23c.js` `leh_ch26b.js` |
| **不写** | §12.2（Czech `10-18`/`10-19` 已覆盖）· §12.4（Czech `10-20` 已覆盖） | — |

⚠️ **这 11 个动手前每一个都按词边界 grep 过 Czech 层，并且有三个的结论不是「零覆盖」——
这三条是这一批真正的产出，别被「11 个全是新内容」这种简化说法覆盖掉：**

| § | 实测 | 处理方式 |
|---|---|---|
| §8.4 | **Czech 覆盖很厚，不是缺口**：cAMP 77 次、NAD 334、FAD 122、ATP 1521。第二信使和核苷酸辅酶都教过 | 只写捷克层没有的那一个论点——**辅酶身上为什么挂着一个不参与反应的核苷酸**（`adenosin` 6 次全是结构性描述）。节点里明说了「本节大部分不是新内容」 |
| §5.2 | **不是零**：`antigen` 在 Czech `2-2-3` 有 5 次，是**一句话**的抗体举例（其 `cz` 字段只写「kontakt podjednotek」）；另 5 次在 `8-2-3-4` 是糖脂抗原标记，属另一主题 | 正因为那句话在，`topicKey` **并进** `protein-tertiary-quaternary`，产生第 21 条跨书连接 |
| §26.4 | **重叠在 Lehninger 层内部，不在 Czech 层**：`ribozyme` 在 `L-26-2-1` 有 6 次、`self-splic` 11 次，还有一整张 trace 卡 | **不重讲「核酶是什么」**，改写「凭什么说它是酶」那五条判据 + RNA 世界假说（`RNA world` 全项目 0 次），并在正文里点名 `L-26-2-1` |

⚠️ **页码：11 个里有 6 个的 `lehninger_only_scope.tsv` 范围是错的**，全部按 A 自己的 OCR 重测过。
§2.1 起点错（tsv 44，实测 43，A 自己的章首目录页 p.43 印着「2.1 … 43 / 2.2 … 53 / 2.3 … 59」）·
§5.2 终点错（164-168 → **164-169**）· §5.3 起点错（168-172 → **169-172**，它和 SUMMARY 5.2
共用 p.169，这就是错的来源）· §9.3 终点错两页（326-334 → **326-336**）· §23.5 终点错
（875-880 → **875-879**，p.880 是 Chapter Review 页不是正文）· §26.4 起点错（996-1001 →
**995-1001**，SUMMARY 26.3 和 26.4 标题共用 p.995）。**「页码必须在 A 自己的 OCR 里测」这条又验证了一次。**

---

## ⚠️ 不要替 Ruojin 判断「哪一节考试相关性高」

**2026-08-10 说过一次头，当场被她问「谁说的」，查完发现没依据。** 原话是「§12.9 癌基因那节
考试相关性最高」——**纯属编的**。

**查证结果，两个方向都不成立：**

1. 仓库里唯一像考纲的东西，是 `HANDOFF.md` 里从她中文笔记目录还原的 **27 个口试题目 +
   若干无编号附录**（抗生素、激素、光合作用、DNA 重组/转座子、复制、转录、RNA 复制、翻译）。
   **癌基因、细胞周期、凋亡、信号转导，一个都不在里面。**
2. **但也不能反过来推「所以不考」**——同一份 handoff 记着她自己的话：
   「中文笔记虽然有光合作用部分，但是**中国是不考的**，但是我在捷克也参加国考的同学**被考了
   光合作用**」。**那份中文笔记目录已经被证伪过一次，它不是捷克国考的可靠地图。**

**所以正确的说法是：这个仓库无法回答「哪一节考试考不考」，能回答的只有 Ruojin。**
这和 CORE3 里那句「命中数只能回答『教材提没提』，不能回答『你的考试考不考』」是同一条规矩，
也和下面 depth queue 那条「ratio 不是重要性评分」是同一个错误形状。**排优先级时，唯一可引用的
依据是她自己说过的话**（例如 ch12 整块「其他的最简单一笔带过就行」），**不是我们对学科的印象。**

## depth queue 剩下的 52 个：不是待办清单

⚠️ **`depth_queue.tsv` 的 ratio 不是重要性评分。** 它是 Lehninger 页数 ÷ Czech 页数，衡量
「Lehninger 多说了多少」。高 ratio 可能是 Czech 书对一个重要东西讲得薄，**也可能**是因为系里
不考它才薄，这个数字分不出两者。已经有一个 session 把 top-20 当成待办队列交出去，Ruojin 很
合理地把剩下的排名读成了欠账。

按 ratio 分档（对**剩余的 52 个**，2026-08-12 重新实测）：**4 个 ≥3.0、22 个在 2.0–3.0、11 个在
1.5–2.0、10 个在 1.0–1.5、5 个 <1.0**（Lehninger 说得并不比 Czech 书多）；ratio ≥1.5 的是 **37 个**。
高档位掉得快是因为 rank 基本按 ratio 排，而 08-12 做掉的是 rank 21–28。
⚠️ **这一行原来结尾写「真实池子约 38 个」，那是算错的，2026-08-10 更正**：当时的分档是
13/25/12/10/5（对剩余 65 个），**13+25+12 = 50，不是 38**——分档表本身一直是对的，错的只有那个合计。
中间态留在这里备查：做掉 §20.6 之后剩 64 个、ratio ≥1.5 是 49 个；光合作用组五节做完后剩 60 个、≥1.5 是 45 个。
**但这个数字本身没有用**——见本节标题那句「ratio 不是重要性评分」。**它只是不该是错的。**

**旧 archive 说剩下有五个高价值主题（遗传密码、蛋白质合成、基因调控、尿素循环、氨基酸降解）
——这个说法是错的，已在 2026-08-08 逐节点 grep 推翻**：`4-2-1` 遗传密码 · `4-2-2`/`4-2-3-1`/
`4-2-3-2` 蛋白质合成 · `4-2-4-1`/`4-2-4-2` 基因调控 · `5-2-2-1`/`5-2-2-2` 尿素循环 · 整个 ch5
氨基酸降解。光合作用同理，ch9 的 27 个节点全是光合，不是缺口。

⚠️ **相信任何覆盖率结论之前，先 grep Czech 层。这条已经打脸过五次**——最后三次都写在 CORE2
自己身上（§24.3 → `4-1-4-1`、§25.2 → `4-1-3-1`、§25.3 → `4-1-2`/`4-1-3-1/2`），全部在
2026-08-08 一天内被推翻。见上面那张三行的对照表。

---

## 建议的下一步

1. ✅ ~~§9.1 克隆/PCR/vector/library~~ —— 已做，`L-9-1-1`。
2. ✅ ~~§9.2 CRISPR~~ —— 已做，`L-9-2-1`，`topicKey: exploring-protein-function`（新起的，
   没有并进 §9.1 的 key——9.1 是「怎么拿到基因和蛋白」，9.2 是「怎么查这个蛋白是干什么的」，
   是两个不同的问题）。
3. ✅ ~~§22.2 氨基酸生物合成~~ —— 已做，`L-22-2-1`，`topicKey: amino-acid-biosynthesis`
   （新起的，故意不合并进已有的 `amino-acids` 或 `amino-acid-degradation`）。
4. ✅ ~~§24.2 DNA supercoiling~~ —— 已做，`L-24-2-1`，`topicKey: dna-supercoiling`（新起的，
   故意不合并进 `4-1-3-1` 的 `dna-replication`）。文件 `leh_ch24.js`，第 24 章的第一个节点。
5. ✅ ~~§24.3 染色质与染色体结构~~ —— 已做，`L-24-3-1`，`topicKey: chromosome-structure`
   （新起的，故意不合并进 §24.2 的 `dna-supercoiling`）。文件 `leh_ch24b.js`。
   **它也是第一个 27 条引用全部通过 `verify_citations.py` 的 Lehninger-only 节点。**
6. ✅ ~~§25.2 DNA repair~~ —— 已做，`L-25-2-1`，`topicKey: dna-repair`（新起的，故意不合并进
   `dna-replication`）。文件 `leh_ch25b.js`，27 条引用全部过检。
7. ✅ ~~§25.3 DNA recombination~~ —— 已做，`L-25-3-1`，`topicKey: dna-recombination`（新起的，
   故意不合并进 `dna-repair`）。文件 `leh_ch25c.js`，28 条引用全部过检。
   **「主要需要的」7 个到此全部完成。**
8. ✅ ~~§19.5 线粒体基因~~ —— 已做，`L-19-5-1`（2026-08-09），文件 `leh_ch19c.js`，
   17 points / 18 terms / 4 quiz / 1 oral，A pp.692–696，**27 条引用全部过检**。
   `topicKey` **并进** `respiratory-chain`（不新起），所以 key 数和跨书连接数**都没变**。
   动手前重新 grep 过 Czech 层，零命中成立。**「merge 进已有 topic」的真缺口只剩 §14.5。**

---

## 下一步：需要 Ruojin 拍板

**支撑「CORE2 优先级最高」的那条理由（她亲自圈定却一个没做的 7 个 section）已经用完了。**
剩下的候选按成本从低到高：

8. ✅ ~~§14.5~~ —— 已做，`L-14-5-1`（2026-08-09），文件 `leh_ch14.js`，
   17 points / 17 terms / 4 quiz / 1 oral，A pp.539–546，**26 条引用全部过检**。
   `topicKey` 并进 `glycolysis`，**产生了第 20 个跨书连接**。**「merge」桶到此空了。**
9. ✅ ~~给 Lehninger 那 30 个节点加 `trace`~~ —— **已于 2026-08-10 收官，30/30，共十批。**
10. ✅ ~~**给 `L-9-1-1`/`L-9-2-1`/`L-22-2-1`/`L-24-2-1` 补行内引用**~~ —— **已于 2026-08-10 全部
    收官，47/47，698 条引用 0 ELSEWHERE 0 UNCHECKED。这一条不用再看了。**
11. 「merge 进已有 topic」**已全部处理完**：三个真缺口都做了；§15.1（2 页）已覆盖、
    §19.4 的主体已在 `L-23-2-1`、§15.3 的「零命中」理由是假的。**这一桶不用再看了。**
12. 「一笔带过」19 个——她的原话是「其他的最简单一笔带过就行」。

⚠️ **但 CORE3（结构式）现在可能更值得先做**：那条线卡在两个**决策**上（可见性、清单分母），
而不是卡在工作量上，而且「画了她看不见」意味着**已完成的工作正在白费**。详见 `CORE_HANDOFF.md`。

✅ **Lehninger 30 个节点全部带 `trace`（2026-08-10 收官）。这一层已经关掉，不是待办。**

**写 Lehninger `trace` 的做法（30 个已确立）——留作将来加新节点时照抄：**
- **一个节点一张卡就够**，挑那个「不讲清楚就说不通」的术语，不要每个术语都写。
  第一批挑的是：`L-14-5-1` → **fructose 2,6-bisphosphate**（因为它不是中间物这件事最反直觉）、
  `L-19-5-1` → **heteroplasmy**（因为核遗传学里没有对应物）、`L-28-3-1` → **转录基态**
  （因为它是「操纵子模型搬不过来」的前提）。
  第三批挑的是：`L-24-2-1` → **linking number (Lk)**（整节的 σ 计算和拓扑异构酶 I/II 型
  分类都从它推出来）、`L-3-3-1` → **specific activity**（它是「纯」的操作性定义，
  而「总活性在降、比活性在升」是整张纯化表的关键句）、`L-1-3-1` → **mass-action ratio Q**
  （不分开 ΔG 和 ΔG°，「ATP 是高能化合物」这个错就没法纠正）。
  **三个都属于同一类：不是最难的术语，而是「不先讲它，后面全变成死记」的那个。** 挑卡时按这条筛。
- 字段就是 Czech 层那五个：`term` / `what` / `from` / `to` / `family` / `numbers`（后两个可选）。
  `to` 一栏要写**考试里具体怎么用**，不要写成总结。
- ⚠️ **`numbers` 里的数字必须来自该节点自己已经过检的正文或原书**，不要凭印象填。
  写这三张时是回 `leh_ch28.js` 正文和 B 的 §14.5 原文里取的数。
- 插入方法：脚本锚定 `    mustKnow: {` 那一行，把 `trace: [...]` 插在它前面，
  **写盘前先 esprima parse**。脚本见提交 `trace-batch-1` 的说明。
  ⚠️ **有些数据文件里有两个节点**（`leh_ch3.js` = `L-3-3-1` + `L-3-4-1`），
  裸锚 `    mustKnow: {` 会命中错的那个。**第三批的做法**：先定位该节点自己的
  `    id: "L-x-y-z",` 行，再取它**之后第一个** `    mustKnow: {`，并断言这两者之间
  **没有另一个 `    id: ` 行**（否则说明锚点已经落到下一个节点里去了）。
  另外顺手断言卡片以 `    ],` 收尾、六个字段各恰好出现一次、引号成对——
  这几条断言比事后 parse 更早报错，能指出是哪个节点写歪了。
- ⚠️ **写之前先把该节点自己的正文 grep 一遍**，第二批就是这么做的：先在节点里搜
  `rotenone`/`antimycin`/`CO`/`catalytic` 这些词，把原句拉出来，卡片再照着写。
  **这样 `numbers` 和结论都落在已过检的文本上，而不是落在印象上。**
  第二批因此拿到了几个很好用的对比：CO 对**游离**血红素的亲和力是氧的 **20,000 倍以上**，
  但**在肌红蛋白里只剩约 40 倍**；草酰乙酸的稳态浓度是**微摩尔级**。

## 写一个 Lehninger-only 节点的现成模板

`biochemie_pro/data/leh_ch9.js`（§9.1）、`leh_ch9b.js`（§9.2）、`leh_ch22b.js`（§22.2）、
`leh_ch24.js`（§24.2）、`leh_ch24b.js`（§24.3）、`leh_ch25b.js`（§25.2）是头六个
Lehninger-only 节点。**照抄 `leh_ch24b.js` 或 `leh_ch25b.js` 最省事**——只有这两个的引用
格式是完全过检的。⚠️ **但如果你写的是「merge 进已有 topic」桶的节点（现在只剩 §14.5），
照抄 `leh_ch28.js` 或 `leh_ch19c.js`**：只有这两个用了并进已有 key + 不写 `Žádná —` 的写法。
`leh_ch19c.js` 还额外做了一件值得照做的事——它在 `coverageNote` 里把**本节特有的 OCR 陷阱**
写了下来（A 把 SUMMARY 印成 `SUMMA RY`；β 在 pp.695–696 一律 OCR 成 `/3` 或 `f3`），
省掉下一个人重新踩一遍。两个约定由它们
确立，后续节点请沿用：`czTitle` 以 **`Žádná —`**（捷克语「无」）开头，表示 Czech 书没有
对应小节；`cnNote.status` 用 **`pending`**，并在 `title` 里**诚实写明为什么没核对**——
中文笔记是 230 页无文字层的手写扫描件，无法自动检索。

字段规模参考（30 个 Lehninger 节点实测）：`points` 7–20、`terms` 6–18、`quiz` 3–6、`oral` 1、
`mustKnow` 1。新建数据文件后**别忘了在 `biochemie_pro/index.html` 里加 `<script>` 标签**，
按章节号排在正确位置——漏了它，节点在磁盘上存在但 app 里根本不加载，而且不报任何错。

⚠️ **页码绝不能从 A↔B 映射插值出来，必须在 A 自己的 OCR 里找到。** 写 `L-24-3-1` 时先把
准备引用的每一句话拿去 A 上搜，**34 句里有 6 句落在别的页上**——SMC 那一整段实际在 A p.908
而不是 909，chromosome territory 在 904 而不是 907。这一步花不到两分钟，直接省掉一轮
ELSEWHERE。现成脚本思路见 `verify_citations.py` 里的 `norm()`：A 是扫描 OCR，比对前必须
去连字、去断词连字符、并把数字间的空格-连字符收拢。

✅ **`leh_ch24b.js` 找到了绕开截断的办法，下次照做：不要一次写整个文件。** 把节点拆成 9 个
片段分别写进 scratchpad（c1 = 头部+标题+coverageNote+mustKnow+summary，c2–c5 = points，
c6–c7 = terms，c8 = quiz，c9 = oral+收尾），每个片段写完立刻 `tail -c 150` 确认它停在
`},` / `],` 这种完整边界上，最后用一个**带断言的 Python 脚本**拼接（断言每块非空、以完整
token 结尾、`id` 恰好出现一次、文件以 `);` 收尾、目标文件不存在），拼完当场 esprima parse。
**82 KB 的节点一次成功，零截断。** 每块 5–15 KB 是安全区间。

⚠️ **写超长节点时留意生成长度，这条规矩已经验证过两次。** `leh_ch9b.js` 在 `points` 数组
中间被截断过一次；`leh_ch22b.js` 一次写完、立刻 parse check，没再出问题；但 `leh_ch24.js`
又在 `oral.model_en` 里断了一次——而且**修复过程本身**也在续写文件里连续断了两次（每次都是
句子写到一半戛然而止），说明这不是某一次的运气问题，是这个环境对**单次输出长度**的硬限制，
和写的是不是完整节点无关。**处置方法**：写完任何大文件（或续写任何大段文字）后，先跑一次
esprima parse check 或者干脆读一遍文件结尾几行，确认句子是完整的、括号引号都闭合了，再继续
做别的事——不要假设 Write 成功、或者续写没报错，就等于内容完整。断在哪就在哪接着写，用
Python 精确拼接（字符串开头结尾先断言匹配再拼），比手动数缩进省事得多。

## 写一个 depth 节点前必读

`topicKey` 是**该节点对应的主 Lehninger section** 的 slug（`master_map.tsv` 里 `leh_sections`
的第一项）。完整对照表 `lehninger_index/topickey_assignment.json`。旧 archive §9c 有选取规则，
需要时 grep 那一节。

⚠️ **`chapter` 是分书局部的**（Czech ch7 是糖，Lehninger ch7 不是）。任何按章节的比较必须先按
`book` 分桶。entity 卡**没有** `chapter` 和 `section`，先判 `kind` 再碰这两个字段。

⚠️ **A 的文字层是扫描 OCR**（`dehy:Jrogenase`、`lnternembrane`），永远不要拿字符串去匹配 A。
搜 B，再换算页码：**A 的印刷页 = A 的 PDF 页 − 36**（612 页上实测，恒定）。
`lehninger_index/scripts/locate.py` 给一个短语就能告诉你它在哪一节、该翻 A 的哪一页。

⚠️ **A↔B 映射是「提议」不是「已验证」**。手工映射与自动匹配器在 113 节里只有 74 节一致
（65%）。写深度内容之前，**先真的翻开那一页确认它讲的就是那件事**。
