# CORE2 depth — 怎么跑才不会一开工就断

**这份文件是 depth 工作的唯一入口，全文 ~90 行，读完再动手。**
它**取代** `CORE2_LEHNINGER.md` 作为开工指引；那份 479 行的文件从现在起只在 dossier
明确指名某一节时才 grep，**不要整读**。原因不是风格，是实测（见下）。

---

## 为什么之前一跑就断

⚠️ **2026-08-12 读转录实测，不是猜的。** 数据在 `~/.claude/projects/*/*.jsonl`，
**不在本仓库**——2026-08-08 判定「查无实据」就是因为在仓库里找。

本项目共 **563 条拒绝记录，`apiRefusalCategory` 全部是 `bio`，563/563 无例外**。

**关键不在数量，在于它们落在哪一步。** 把每次拒绝的前一步拉出来看：

| 拒绝时，上一步在做什么 |
|---|
| `Get-ChildItem` —— 就是列一个目录 |
| `git status` · `git commit` · `git ls-files` |
| `check_structures.py` · `verify_citations.py` |
| 用户只打了一句「继续」 |

**没有一次落在写生化内容的那一刻。** 有一次的上一步字面上就是「列出 `bio-state-exam` 顶层目录」。

**结论：触发的是已经累积起来的上下文，不是当前那句话。**
所以**改措辞、换说法、把内容写得「更安全」全部无效**——已经为此烧掉过两个 session，不要再试。

**而 depth 的旧流程恰好是「写之前先把生化文本大量灌进来」**：
读 CORE2（479 行）→ grep 302 KB 的 archive → 打开教材核对引用。
等准备工作做完，上下文已经泡透了，**一条内容还没写**。这就是「一跑就中断」的机制。

**对照组：CORE3 一口气跑完 217 个结构式**，因为它整场都在处理 CID、分子式、坐标——
**化学在数据里，不在上下文里。** 下面这套就是把 depth 改成同一个形状。

⚠️ **模型选择帮不上忙，别再往那个方向想。**
`fable-5` 的拒绝有 86/101 被自动回退救回；`opus-5` 227 次、`sonnet-5` 190 次、
`opus-5[1m]` 45 次，**合计 462 次救回 0 次**（已经在最高档，没有更高档可退）。
但**模型是系统自己切的，不由使用者控制**，所以这条只能解释现象，不能当方案。

---

## 怎么跑

**两个 session，分工是硬的：准备阶段不写字，写作阶段不读书。**

### Session A — 准备（跑脚本，不写内容）

```bash
python lehninger_index/scripts/prep_depth.py 20.2
# 想换覆盖探针词：      --terms "S-state,Mn4CaO5,exciton"
# 引语不够用时放大上限：--max 165
```

它自己去读 `depth_queue.tsv`、教材那几页、以及全部 262 个节点，
产出 `lehninger_index/dossiers/_dossier_<section>.md`（约 **12 KB**）。
**教材、archive、CORE2 全文都只经过脚本，不进任何人的上下文。**

⚠️ dossier 不入库（`.gitignore` 已加）——它是可随时重生成的中间产物，不是资料。

### Session B — 写作（全新 session，开场只说这一句）

```
读 lehninger_index/dossiers/_dossier_<section>.md，按里面的范围写 L-<section>-1 这个节点，
写完立刻 commit + push。
不要读 CORE2_LEHNINGER.md / HANDOFF_LEHNINGER.md / CORE_HANDOFF.md，不要打开教材。
```

**一个节点一提交，不要攒批。** 这一轮实测有效：中途断过一次，回「继续」接上，什么都没丢。

**断了就回「继续」。** 不要花 session 研究怎么绕过去。

---

## dossier 里有什么

| 段落 | 内容 | 怎么用 |
|---|---|---|
| 抬头 | 页码范围、queue rank/ratio、指向本节的捷克 section | ⚠️ 页码来自 tsv，**已经错过 7 次**，引用前确认首末页真属于本节 |
| 捷克层已有什么 | 关键词探针命中的节点 id | ⚠️ **这是关键词探针，不是覆盖结论**。本项目的覆盖判断**已被推翻五次**，全部是「没查就填」。命中可能是假阳性，落空也可能只是换了说法 |
| topicKey | 现有全部 key（折叠） | **并进已有 key，不要新起**。并进后跨书连接数可能涨也可能不涨，两种都正常 |
| 候选引语 | 已通过 `verify_citations.py` 探针规则的句子，带页码 | 照抄任一条 + 页码，**不可能**产生 UNCHECKED 行 |

**引语已经预先滤掉三个坑**（每一个都会让**完全正确的引用**被报成不可验证）：
超过 140 字符 · 含 `−`（U+2212，A 的 OCR 里没有这个字符）· 嵌套引号。
**你自己另写的引语必须满足同样规则。**

⚠️ **候选引语是截断过的**（每页留 3 条、总共 55 条），dossier 里会明说丢了多少条。
**不够用就用 `--max` 重跑，不要因此打开教材**——打开教材就等于放弃了这套方案。

---

## 写节点时仍然生效的硬规矩

这几条来自 Ruojin 本人，dossier 里不重复，但它们不因为换了流程就失效：

1. **有机化学必须落到可观察的后果上。** 原话：我的有机化学几乎说是没有根基…一定不要讲完全
   化学，我听不懂的。不是「4n+2 的离域 π 体系」，而是「环是平的，所以在 280 nm 吸收，
   所以能拿来测蛋白浓度」。
2. **捷克教材定范围。** 中文笔记只是理解辅助，从不定义什么可考。同学报告的真实考题最大。
3. **`mustKnow` 不是摘要**，是「忘光之后你仍然希望留下的那句」。
4. **`trace` 的语气**：先说清这东西**为什么存在**，再说它是什么。像科普，有温度。
   最高产的一张卡是**教材推翻了读者已有印象**的那种。
5. **绝不凭记忆写参考数据。** 取来、连出处存、用前断言。这一轮刚靠它抓出一个真错误
   （NADH 那条写「差一个 H」，分子式实际差两个）。

⚠️ **`explain` / `skipIt` / `bank` 有意留空，不要去填。**
⚠️ **浅节点（`brief` 桶）只带 `summary` + `mustKnow`，是合法规格，不要「后来补全」。**

---

## 收工

```bash
python lehninger_index/scripts/step5_check.py        # 结论在 _step5_report.txt，只看到 done 不算
python lehninger_index/scripts/verify_citations.py   # 明细在 _citation_audit.txt
python lehninger_index/scripts/audit_handoff.py      # 当前基线 TOTAL: 3，都在旧 archive 里
```

⚠️ **前两个脚本屏幕上只印一个 `done`**，真正的结论写在各自的报告文件里，**跑完要 tail**。

---

## 这套方案的诚实边界

**它不保证不断。** 题材本身就是触发源，仓库里任何东西都改变不了这一点。

它能保证的是两件事，都是可验证的：
**准备阶段不再消耗 session**（教材和 archive 由脚本读，一次性产出 12 KB），
以及**断点更可能落在已提交一个节点之后**，而不是像现在这样落在还没开始的时候。

**这是把中断从「损失一个 session」降成「损失几十秒」，不是消灭中断。**
